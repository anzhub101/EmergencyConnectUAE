# Secrets — never commit
.env
**/.env
!**/.env.example

# TLS keystores / private keys — keep local, never commit
*.p12
*.jks
*.key
*.pem

# Java / Maven
backend/target/
*.class

# Node / Vite
frontend/node_modules/
frontend/dist/
node_modules/

# OS / IDE
.DS_Store
*.log
